@using Microsoft.AspNet.Identity
@if (Request.IsAuthenticated)
{
    using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm", @class = "navbar-right" }))
    {
        @Html.AntiForgeryToken()

        <ul class="nav navbar-nav navbar-right">
            @* @Html.ActionLink("Hello " + User.Identity.GetUserName() + "!", "Manage", "Account", routeValues: null, htmlAttributes: new { title = "Manage" });*@
            <li class="dropdown profile_details_drop" id="profile">
                <a class="dropdown-toggle" data-toggle="dropdown" href="#">
                    Hello @User.Identity.GetUserName().ToString() !
                    <i class="fa fa-caret-down"></i>
                    <i class="fa fa-caret-up"></i>
                </a>
                <ul class="dropdown-menu">
                    @if (User.IsInRole("Administrator"))
                    {
                        <li> <a href="@Url.Action("Index","Admin")"><i class="fa fa-cog"></i> Admin Settings</a> </li>
                    }
                    else
                    {
                        <li> <a href="@Url.Action("History", "Account")"><i class="fa fa-clock-o"></i> Purchase history</a> </li>
                        <li> <a href="@Url.Action("ManageInfo","Account")"><i class="fa fa-user"></i> Profile</a> </li>
                    }
                    <li> <a href="javascript:document.getElementById('logoutForm').submit()"><i class="fa fa-sign-out"></i> Logout</a> </li>
                </ul>
<li>
            <li id="cart" style="cursor: pointer"><a data-toggle="modal" data-target="#myModal"><span id="soluong" style="margin-top: -25px; font-size: 9px" class="badge">0</span><span class="glyphicon glyphicon-shopping-cart"></span> Cart</a></li>
        </ul>
    }
}
else
{
    <ul class="nav navbar-nav navbar-right">

        @*<li>@Html.ActionLink("Register", "Register", "Account", routeValues: null, htmlAttributes: new { id = "registerLink" })</li>
            <li id="account" style="cursor: pointer"><a href="@Html.Action("Login", "Account")" id="loginLink"><span class="glyphicon glyphicon-log-in"></span> Đăng Nhập</a></li>
            <li id="cart" style="cursor: pointer"><a data-toggle="modal" data-target="#myModal"><span id="soluong" style="margin-top: -25px; font-size: 9px" class="badge">0</span><span class="glyphicon glyphicon-shopping-cart"></span> Cart</a></li>*@
        @*<li id="register">@Html.ActionLink("Register", "Register", "Account", routeValues: null, htmlAttributes: new { id = "registerLink" })</li>*@
        <li id="register"><a id="registerLink" href="@Url.Action("Register", "Account")"><span class="glyphicon glyphicon-user"></span> Register</a></li>
        @*<li id="login">@Html.ActionLink("Log in", "Login", "Account", routeValues: null, htmlAttributes: new { id = "loginLink" })</li>*@
        <li id="login"><a id="registerLink" href="@Url.Action("Login","Account")"><span class="glyphicon glyphicon-log-in"></span> Log in</a></li>

        <li id="cart" style="cursor: pointer"><a data-toggle="modal" data-target="#myModal"><span id="soluong" style="margin-top: -25px; font-size: 9px" class="badge">0</span><span class="glyphicon glyphicon-shopping-cart"></span> Cart</a></li>
    </ul>
    @*<ul class="nav navbar-nav navbar-right text-center">
            <li id="account" style="cursor: pointer"><a><span class="glyphicon glyphicon-log-in"></span> Đăng Nhập</a></li>
            <li id="cart"><a data-toggle="modal" data-target="#myModal"><span id="soluong" style="margin-top: -25px; font-size: 9px" class="badge">0</span><span class="glyphicon glyphicon-shopping-cart"></span> Cart</a></li>
        </ul>*@
}
